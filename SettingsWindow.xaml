<Window x:Class="LiveCaptioner.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:LiveCaptioner.Localization"
        Title="{loc:Loc SettingsWindowTitle}"
        Width="860"
        Height="640"
        MinWidth="780"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="#111318">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E9EEF6" />
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB4C3" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="BorderBrush" Value="#344052" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="BorderBrush" Value="#344052" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="BorderBrush" Value="#344052" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="MaxDropDownHeight" Value="260" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="ToggleBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="6">
                                            <Grid>
                                                <ContentPresenter x:Name="ContentSite"
                                                                  Margin="11,0,34,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  RecognizesAccessKey="True"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" />
                                                <Path Width="8"
                                                      Height="5"
                                                      Margin="0,0,12,0"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Fill="#AAB4C3"
                                                      Stretch="Fill"
                                                      Data="M 0 0 L 4 5 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#4B5F78" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#3B82F6" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="ToggleBorder" Property="Opacity" Value="0.55" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Margin="0,4,0,0"
                                            Padding="4"
                                            Background="#1B2029"
                                            BorderBrush="#3B82F6"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                        <ScrollViewer CanContentScroll="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>

                            <TextBox x:Name="PART_EditableTextBox"
                                     Margin="11,0,34,0"
                                     Padding="0"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Left"
                                     Background="Transparent"
                                     Foreground="{TemplateBinding Foreground}"
                                     BorderThickness="0"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"
                                     Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, UpdateSourceTrigger=PropertyChanged}"
                                     Visibility="Hidden" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Popup" Property="MinHeight" Value="38" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                <Setter TargetName="ToggleButton" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#243044" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2563EB" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E9EEF6" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="MinWidth" Value="86" />
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#3B82F6" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#171B22"
                BorderBrush="#232B36"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="18">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="LiveCaptioner"
                               FontSize="20"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{loc:Loc SettingsSubtitle}"
                               Foreground="#7F8B9D"
                               Margin="0,4,0,22" />
                </StackPanel>

                <ListBox x:Name="NavList"
                         SelectionChanged="NavList_SelectionChanged"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="#E9EEF6"
                         SelectedIndex="0">
                    <ListBoxItem Content="{loc:Loc General}" Padding="10,9" />
                    <ListBoxItem Content="{loc:Loc SttRecognition}" Padding="10,9" />
                    <ListBoxItem Content="{loc:Loc Translation}" Padding="10,9" />
                    <ListBoxItem Content="{loc:Loc Appearance}" Padding="10,9" />
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Margin="26"
                    Padding="0"
                    Background="#111318">
                <Grid>
                    <Grid x:Name="GeneralPage">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="{loc:Loc General}"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,20" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Language}" />
                                <ComboBox x:Name="LanguageBox" Margin="0,0,0,16">
                                    <ComboBoxItem Content="{loc:Loc LanguageSimplifiedChinese}" Tag="zh-CN" />
                                    <ComboBoxItem Content="{loc:Loc LanguageEnglish}" Tag="en" />
                                </ComboBox>
                                <CheckBox x:Name="AutoTranslateBox" Content="{loc:Loc AutoTranslate}" />
                                <CheckBox x:Name="BilingualComparisonBox" Content="{loc:Loc BilingualComparison}" />
                                <CheckBox x:Name="SaveHistoryBox" Content="{loc:Loc SaveHistory}" />
                                <CheckBox x:Name="ClickThroughBox" Content="{loc:Loc ClickThrough}" ToolTip="{loc:Loc ClickThroughToolTip}" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc MinTextLength}" Margin="0,12,0,6" />
                                <TextBox x:Name="MinTextLengthBox" Margin="0,0,0,12" Width="120" HorizontalAlignment="Left" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc DebounceDelayMs}" />
                                <TextBox x:Name="DebounceDelayBox" Margin="0,0,0,12" Width="120" HorizontalAlignment="Left" />
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="SttPage" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="{loc:Loc SttRecognition}"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,20" />

                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc RecognitionMethod}" />
                                <ComboBox x:Name="AsrProviderBox" Margin="0,0,0,16">
                                    <ComboBoxItem Content="assemblyai" />
                                    <ComboBoxItem Content="azure" />
                                    <ComboBoxItem Content="google" />
                                    <ComboBoxItem Content="custom" />
                                    <ComboBoxItem Content="local" />
                                </ComboBox>

                                <TextBlock Text="AssemblyAI" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc ApiKey}" />
                                <PasswordBox x:Name="AssemblyAiKeyBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Model}" />
                                <TextBox x:Name="AssemblyAiModelBox" Margin="0,0,0,18" />

                                <TextBlock Text="Azure Speech" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc SpeechKey}" />
                                <PasswordBox x:Name="AzureSpeechKeyBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Region}" />
                                <TextBox x:Name="AzureSpeechRegionBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Language}" />
                                <TextBox x:Name="AzureSpeechLanguageBox" Margin="0,0,0,18" />

                                <TextBlock Text="Google Speech" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc GoogleCredentialsPath}" />
                                <TextBox x:Name="GoogleCredentialsPathBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Language}" />
                                <TextBox x:Name="GoogleSpeechLanguageBox" Margin="0,0,0,18" />

                                <TextBlock Text="{loc:Loc LocalWhisper}" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Model}" />
                                <ComboBox x:Name="WhisperModelBox" Margin="0,0,0,12">
                                    <ComboBoxItem Content="tiny" />
                                    <ComboBoxItem Content="base" />
                                    <ComboBoxItem Content="small" />
                                </ComboBox>
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Backend}" />
                                <ComboBox x:Name="AsrBackendBox" Margin="0,0,0,18">
                                    <ComboBoxItem Content="auto" />
                                    <ComboBoxItem Content="cuda" />
                                    <ComboBoxItem Content="vulkan" />
                                    <ComboBoxItem Content="cpu" />
                                </ComboBox>

                                <TextBlock Text="{loc:Loc CustomStt}" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc WebSocketUrl}" />
                                <TextBox x:Name="CustomSttUrlBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc ApiKey}" />
                                <PasswordBox x:Name="CustomSttApiKeyBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc AuthHeader}" />
                                <TextBox x:Name="CustomSttAuthHeaderBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc TranscriptJsonPath}" />
                                <TextBox x:Name="CustomSttTranscriptFieldBox" />
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="LlmPage" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="{loc:Loc Translation}"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,20" />

                                <!-- ── Shared: translation provider + target language ── -->
                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc TranslationProvider}" />
                                <ComboBox x:Name="TranslationProviderBox" Margin="0,0,0,12" SelectionChanged="TranslationProviderBox_SelectionChanged">
                                    <ComboBoxItem Content="{loc:Loc TranslationProviderLLM}" Tag="llm" />
                                    <ComboBoxItem Content="{loc:Loc TranslationProviderGoogle}" Tag="google" />
                                    <ComboBoxItem Content="{loc:Loc TranslationProviderMicrosoft}" Tag="microsoft" />
                                </ComboBox>

                                <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc TargetLanguage}" />
                                <ComboBox x:Name="TargetLanguageBox" Margin="0,0,0,16">
                                    <ComboBoxItem Content="简体中文" Tag="zh-CN" />
                                    <ComboBoxItem Content="繁體中文" Tag="zh-TW" />
                                    <ComboBoxItem Content="English" Tag="en" />
                                    <ComboBoxItem Content="日本語" Tag="ja" />
                                    <ComboBoxItem Content="한국어" Tag="ko" />
                                    <ComboBoxItem Content="Français" Tag="fr" />
                                    <ComboBoxItem Content="Deutsch" Tag="de" />
                                    <ComboBoxItem Content="Español" Tag="es" />
                                    <ComboBoxItem Content="Português" Tag="pt" />
                                    <ComboBoxItem Content="Русский" Tag="ru" />
                                    <ComboBoxItem Content="العربية" Tag="ar" />
                                    <ComboBoxItem Content="ไทย" Tag="th" />
                                    <ComboBoxItem Content="Tiếng Việt" Tag="vi" />
                                </ComboBox>

                                <!-- ── LLM-specific settings ── -->
                                <StackPanel x:Name="LlmSettingsPanel">
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Provider}" />
                                    <ComboBox x:Name="LlmProviderBox" Margin="0,0,0,12" SelectionChanged="LlmProviderBox_SelectionChanged">
                                        <ComboBoxItem Content="deepseek" />
                                        <ComboBoxItem Content="openai" />
                                        <ComboBoxItem Content="claude" />
                                        <ComboBoxItem Content="custom-openai" />
                                    </ComboBox>
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc ApiKey}" />
                                    <PasswordBox x:Name="LlmApiKeyBox" Margin="0,0,0,12" />
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc ApiUrlBaseUrl}" />
                                    <TextBox x:Name="LlmBaseUrlBox" Margin="0,0,0,12" />
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Model}" />
                                    <ComboBox x:Name="LlmModelBox" IsEditable="True" Margin="0,0,0,12" />
                                </StackPanel>

                                <!-- ── Google-specific settings ── -->
                                <StackPanel x:Name="GoogleSettingsPanel" Visibility="Collapsed">
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc ApiKey}" />
                                    <PasswordBox x:Name="GoogleTranslateKeyBox" Margin="0,0,0,12" />
                                </StackPanel>

                                <!-- ── Microsoft-specific settings ── -->
                                <StackPanel x:Name="MicrosoftSettingsPanel" Visibility="Collapsed">
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc ApiKey}" />
                                    <PasswordBox x:Name="MicrosoftTranslatorKeyBox" Margin="0,0,0,12" />
                                    <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc Region}" />
                                    <TextBox x:Name="MicrosoftTranslatorRegionBox" Margin="0,0,0,12" />
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="AppearancePage" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Appearance}"
                                       FontSize="26"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,20" />
                            <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc FontSize}" />
                            <Slider x:Name="FontSizeSlider"
                                    Minimum="16"
                                    Maximum="72"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    Margin="0,0,0,22" />
                            <TextBlock Style="{StaticResource LabelText}" Text="{loc:Loc BackgroundOpacity}" />
                            <Slider x:Name="OpacitySlider"
                                    Minimum="0.15"
                                    Maximum="0.9"
                                    TickFrequency="0.05" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Padding="18"
                    BorderBrush="#232B36"
                    BorderThickness="0,1,0,0"
                    Background="#151922">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{loc:Loc Cancel}" Margin="0,0,10,0" Click="CancelButton_Click" />
                    <Button Content="{loc:Loc Save}" Click="SaveButton_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>

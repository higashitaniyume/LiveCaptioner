<Window x:Class="LiveCaptioner.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LiveCaptioner 设置"
        Width="860"
        Height="640"
        MinWidth="780"
        MinHeight="560"
        WindowStartupLocation="CenterOwner"
        Background="#111318">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E9EEF6" />
        </Style>
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAB4C3" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="BorderBrush" Value="#344052" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="BorderBrush" Value="#344052" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="BorderBrush" Value="#344052" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinHeight" Value="38" />
            <Setter Property="MaxDropDownHeight" Value="260" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="ToggleBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="6">
                                            <Grid>
                                                <ContentPresenter Margin="11,0,34,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  RecognizesAccessKey="True"
                                                                  Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" />
                                                <Path Width="8"
                                                      Height="5"
                                                      Margin="0,0,12,0"
                                                      HorizontalAlignment="Right"
                                                      VerticalAlignment="Center"
                                                      Fill="#AAB4C3"
                                                      Stretch="Fill"
                                                      Data="M 0 0 L 4 5 L 8 0 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#4B5F78" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="#3B82F6" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="ToggleBorder" Property="Opacity" Value="0.55" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Margin="0,4,0,0"
                                            Padding="4"
                                            Background="#1B2029"
                                            BorderBrush="#3B82F6"
                                            BorderThickness="1"
                                            CornerRadius="6">
                                        <ScrollViewer CanContentScroll="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Popup" Property="MinHeight" Value="38" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Background" Value="#1B2029" />
            <Setter Property="Foreground" Value="#F5F7FA" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#243044" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2563EB" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E9EEF6" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="18,9" />
            <Setter Property="MinWidth" Value="86" />
            <Setter Property="Background" Value="#2563EB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#3B82F6" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#171B22"
                BorderBrush="#232B36"
                BorderThickness="0,0,1,0">
            <DockPanel Margin="18">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="LiveCaptioner"
                               FontSize="20"
                               FontWeight="SemiBold" />
                    <TextBlock Text="字幕管线设置"
                               Foreground="#7F8B9D"
                               Margin="0,4,0,22" />
                </StackPanel>

                <ListBox x:Name="NavList"
                         SelectionChanged="NavList_SelectionChanged"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="#E9EEF6"
                         SelectedIndex="0">
                    <ListBoxItem Content="通用" Padding="10,9" />
                    <ListBoxItem Content="STT 识别" Padding="10,9" />
                    <ListBoxItem Content="LLM 模型" Padding="10,9" />
                    <ListBoxItem Content="外观" Padding="10,9" />
                </ListBox>
            </DockPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Margin="26"
                    Padding="0"
                    Background="#111318">
                <Grid>
                    <Grid x:Name="GeneralPage">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="通用"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,20" />
                                <CheckBox x:Name="AutoTranslateBox" Content="自动润色 / 翻译" />
                                <CheckBox x:Name="BilingualComparisonBox" Content="实时双语对照" />
                                <CheckBox x:Name="SaveHistoryBox" Content="保存字幕历史" />
                                <CheckBox x:Name="ClickThroughBox" Content="鼠标穿透" ToolTip="启用后可用 Ctrl+Alt+C 或托盘菜单恢复交互" />
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="SttPage" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="STT 识别"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,20" />

                                <TextBlock Style="{StaticResource LabelText}" Text="识别方式" />
                                <ComboBox x:Name="AsrProviderBox" Margin="0,0,0,16">
                                    <ComboBoxItem Content="assemblyai" />
                                    <ComboBoxItem Content="azure" />
                                    <ComboBoxItem Content="google" />
                                    <ComboBoxItem Content="custom" />
                                    <ComboBoxItem Content="local" />
                                </ComboBox>

                                <TextBlock Text="AssemblyAI" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="API Key" />
                                <PasswordBox x:Name="AssemblyAiKeyBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="模型" />
                                <TextBox x:Name="AssemblyAiModelBox" Margin="0,0,0,18" />

                                <TextBlock Text="Azure Speech" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="Speech Key" />
                                <PasswordBox x:Name="AzureSpeechKeyBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="Region" />
                                <TextBox x:Name="AzureSpeechRegionBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="语言" />
                                <TextBox x:Name="AzureSpeechLanguageBox" Margin="0,0,0,18" />

                                <TextBlock Text="Google Speech" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="服务账号 JSON 路径" />
                                <TextBox x:Name="GoogleCredentialsPathBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="语言" />
                                <TextBox x:Name="GoogleSpeechLanguageBox" Margin="0,0,0,18" />

                                <TextBlock Text="本地 Whisper" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="模型" />
                                <ComboBox x:Name="WhisperModelBox" Margin="0,0,0,12">
                                    <ComboBoxItem Content="tiny" />
                                    <ComboBoxItem Content="base" />
                                    <ComboBoxItem Content="small" />
                                </ComboBox>
                                <TextBlock Style="{StaticResource LabelText}" Text="后端" />
                                <ComboBox x:Name="AsrBackendBox" Margin="0,0,0,18">
                                    <ComboBoxItem Content="auto" />
                                    <ComboBoxItem Content="cuda" />
                                    <ComboBoxItem Content="vulkan" />
                                    <ComboBoxItem Content="cpu" />
                                </ComboBox>

                                <TextBlock Text="自定义 STT" FontSize="16" FontWeight="SemiBold" Margin="0,8,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="WebSocket URL" />
                                <TextBox x:Name="CustomSttUrlBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="API Key" />
                                <PasswordBox x:Name="CustomSttApiKeyBox" />
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="LlmPage" Visibility="Collapsed">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="LLM 模型"
                                           FontSize="26"
                                           FontWeight="SemiBold"
                                           Margin="0,0,0,20" />
                                <TextBlock Style="{StaticResource LabelText}" Text="提供商" />
                                <ComboBox x:Name="LlmProviderBox" Margin="0,0,0,12">
                                    <ComboBoxItem Content="deepseek" />
                                    <ComboBoxItem Content="openai" />
                                    <ComboBoxItem Content="claude" />
                                    <ComboBoxItem Content="custom-openai" />
                                </ComboBox>
                                <TextBlock Style="{StaticResource LabelText}" Text="API Key" />
                                <PasswordBox x:Name="LlmApiKeyBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="API URL / Base URL" />
                                <TextBox x:Name="LlmBaseUrlBox" Margin="0,0,0,12" />
                                <TextBlock Style="{StaticResource LabelText}" Text="模型" />
                                <TextBox x:Name="LlmModelBox" />
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid x:Name="AppearancePage" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="外观"
                                       FontSize="26"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,20" />
                            <TextBlock Style="{StaticResource LabelText}" Text="字体大小" />
                            <Slider x:Name="FontSizeSlider"
                                    Minimum="16"
                                    Maximum="72"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    Margin="0,0,0,22" />
                            <TextBlock Style="{StaticResource LabelText}" Text="背景透明度" />
                            <Slider x:Name="OpacitySlider"
                                    Minimum="0.15"
                                    Maximum="0.9"
                                    TickFrequency="0.05" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Padding="18"
                    BorderBrush="#232B36"
                    BorderThickness="0,1,0,0"
                    Background="#151922">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Margin="0,0,10,0" Click="CancelButton_Click" />
                    <Button Content="保存" Click="SaveButton_Click" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
